---
// const response = await fetch("https://jsonplaceholder.typicode.com/posts/1/coments");
// const stuff = await response.json()

// interface responseData {
//   postId: number
//   id: number
//   name: string
//   email: string
//   body: string
// }

import Layout from "../layouts/Layout.astro";
import H1 from "../components/H1.astro";
import Main from "../components/Main.astro";

export const prerender = true;
---

<Layout title="Welcome to spaced repetition by SOGGY INK GAMES">
  <Main>
    <H1 text="Your Notes" />
    <!-- {stuff.map((item: responseData ) => 
    <ul>
      <li>Post Name:{item.name}</li>
      <li>Email:{item.name}</li>
      <li>Text:{item.name}</li>      
    </ul>
    )} -->
  </Main>
</Layout>

